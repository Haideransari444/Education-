# Integrations

`vendor/` preserves a reviewed upstream snapshot. `src/integrations/` contains the AI Core-adapted implementation that connects approved upstream work to the local lesson architecture.

Integrated educational modules must not depend on the upstream demo or upstream website remaining online.
